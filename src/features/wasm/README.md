# WASM Feature Slot

Use this folder for optional browser-side WebAssembly modules.

Recommended use cases:
- client-side parsing/transforms
- image/audio pre-processing
- CPU-heavy utilities that should not run in serverless functions
